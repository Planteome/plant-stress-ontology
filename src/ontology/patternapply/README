Edit the curie_map.yaml to reflect the URLs
Add diseases to test_aps_disease.tsv in the format specified in that file.
modify the design pattern in design-pattern.yaml


Run this:
    apply-pattern.py -P curie_map.yaml -b http://purl.obolibrary.org/obo/ -i test_aps_diseases.tsv -p design_pattern.yaml > OOPS.owl
    ../../../bin/owltools OOPS.owl 
